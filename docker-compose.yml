version: '3.8'

services:
  cloudmail-frontend:
    build: .
    container_name: cloudmail-frontend
    restart: unless-stopped
    ports:
      - "8080:80"
    # 如果要用 HTTPS，可以在这里挂载证书
    # volumes:
    #   - ./ssl:/etc/nginx/ssl:ro
