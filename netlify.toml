# Cloudflare Pages 部署配置
# 构建命令: npm run build
# 输出目录: out
# 框架预设: Next.js (Static HTML Export)

[build]
  command = "npm run build"
  publish = "out"

[build.environment]
  NODE_VERSION = "18"

# 重定向规则 - 所有路由回退到 index.html（SPA 模式）
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
